<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Uy sahifasi</title>
  </head>
  <body>
    <header>
      <nav>
        <%if(!name){ %>
        <a href="/register">Ro'yhatdan o'tish</a>
        <a href="/login">Tizimga kirish</a>
        <% } else { %>
        <p><%= name %></p>
        <button onclick="() => {localStorage.removeItem('x-token')}">
          Chiqish
        </button>
        <% } %>
      </nav>
    </header>
  </body>
</html>
